23:> determinista: dois renders da mesma cena na mesma maquina produzem bytes
24:> diferentes (medido; ver AB-396). Sob webm, `--conferir` sai VERMELHO por
28:> so volta com um encoder determinista, e a pergunta esta aberta em AB-396.
103:- O libvpx-vp9 desta cadeia **nao e determinista**: dois renders da mesma
104:  cena, mesma maquina, mesmo job, produzem bytes diferentes (AB-396). O
197:deixou de ser reproduzivel: o libvpx-vp9 desta cadeia nao e determinista,
199:de hash de `resultado.json` e `procedencia.json` (**AB-396**). A cadeia
247:4. **O webm nao e determinista** (libvpx-vp9, AB-396): o hash do asset
250:   encoder/saida determinista e e uma pergunta aberta.
