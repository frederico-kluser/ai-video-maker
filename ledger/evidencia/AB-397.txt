1:# ADR-0009: Estagio de resolucao `grafico` — Manim headless, quirks do 3b1b e formato de alfa
97:- `-t --format=webm` no Manim 0.20.1 + PyAV 18 produz **VP9 yuv420p** —
99:  alfa e descartado**: a expectativa "vp9/yuva420p" do registro AB-390 foi
100:  falsificada empiricamente. O libvpx-vp9 desta cadeia nao carrega alfa:
101:  ate o ffmpeg CLI com `-pix_fmt yuva420p` sai yuv420p, e vp8/yuva420p
102:  falha com `avcodec_open2` 22. O `.mov` qtrle/argb do v1.0.0 TINHA alfa.
112:  ou nao o alfa e do container — hoje nao carrega (AB-390 aberto com a
113:  consequencia: o grafico composto sobre a cena vem com retangulo preto,
120:  **AB-390** (agora: webm reproduz, alfa ausente), verificado no join da
226:   do no F1-09 aceita `video/webm` (alfa declarado: true na tabela, mas o
227:   bitstream real sai yuv420p — ver AB-390).
243:3. **O webm nao carrega alfa nesta cadeia** (VP9 yuv420p, medido): o
244:   grafico composto sobre a cena vem como retangulo preto, nao
