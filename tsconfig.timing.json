{
  "//": "Card F3-01. Type-check ESCOPADO em src/sincronia/timing/ e nos seus testes.",
  "//2": "Existe para o gate `just timing-testar` reprovar por causa DESTE card,",
  "//3": "nao por causa de outro. O type-check do repo inteiro continua em",
  "//4": "`just typecheck` / `just build` — este arquivo nao substitui aquele.",
  "extends": "./tsconfig.json",
  "include": [
    "src/sincronia/timing/**/*.ts",
    "tests/sincronia/**/*.ts",
    "tools/timing/**/*.ts"
  ]
}
